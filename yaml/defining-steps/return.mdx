---
title: "return"
description: "Returns a value and ends the task execution"
---

### Example[](#example "Direct link to Example")

```yaml
main:
  name: Send a demo PUT request
  input:
    name:
      type: string
  steps:
    - name: Return a result
      return: ${"Hello World!"}
```

### Parameters[](#parameters "Direct link to Parameters")

<ResponseField name="return" required>
  A value to return
</ResponseField>
